(jbuild_version 1)

(executables
 ((names (main))
  (public_names (price-tracker-exe))
  (modules (main))
  (libraries (price-tracker-lib))
  (preprocess (pps (lwt_ppx)))
  (package price-tracker-exe)))
