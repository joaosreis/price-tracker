(jbuild_version 1)

(executables
 ((names (main))
  (public_names (price-tracker-exe))
  (modules (main))
  (libraries (price-tracker-lib cmdliner ppx_blob))
  (preprocess (pps (lwt.ppx ppx_blob)))
  (package price-tracker-exe)))
